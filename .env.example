# LOCAL DEVELOPMENT ENV (DO NOT COMMIT REAL SECRETS)
# Fill in the placeholders below with your real values.
# Copy of .env.example plus inline TODO markers.

# --- Core Feature Flags ---
EBAY_PUBLISH_ENABLED=true
EBAY_SNAPSHOTS_ENABLED=true
EBAY_RECON_ENABLED=false           # enable later after initial publishes (set true to test reconciliation)
EBAY_POLICY_ENABLED=false          # set true if you want to fetch policies immediately

# --- eBay API (Sandbox) ---
EBAY_API_BASE_URL=https://api.sandbox.ebay.com
EBAY_API_OAUTH_TOKEN=__FILL_ACCESS_TOKEN__        # Paste short-lived OAuth user access token when obtained
EBAY_API_REFRESH_URL=https://api.sandbox.ebay.com/identity/v1/oauth2/token
EBAY_API_REFRESH_CLIENT=__FILL_EBAY_REFRESH_CLIENT__
EBAY_API_REFRESH_SECRET=__FILL_EBAY_REFRESH_SECRET__
EBAY_API_REFRESH_TOKEN=__FILL_REFRESH_TOKEN__     # Obtain a long‑lived refresh token (user consent flow)
EBAY_API_DEV_ID=__FILL_EBAY_DEV_ID__

# --- OAuth (tokenManager expects these names) ---
EBAY_OAUTH_CLIENT_ID=__FILL_EBAY_OAUTH_CLIENT_ID__
EBAY_OAUTH_CLIENT_SECRET=__FILL_EBAY_OAUTH_CLIENT_SECRET__
EBAY_OAUTH_REFRESH_TOKEN=__FILL_REFRESH_TOKEN__
EBAY_OAUTH_TOKEN_URL=https://api.sandbox.ebay.com/identity/v1/oauth2/token
EBAY_OAUTH_SCOPE=https://api.ebay.com/oauth/api_scope # add additional space‑delimited scopes as needed

# --- Adapter Behavior ---
EBAY_API_TIMEOUT_MS=8000
EBAY_RATE_LIMIT_RPS=5
EBAY_PUBLISH_MAX_ATTEMPTS=6

# --- Queue / Alert Thresholds ---
EBAY_QUEUE_BACKLOG_READY_THRESHOLD=50
QUEUE_WAIT_SAMPLES_CAP=2000
QUEUE_MAX_WAIT_DECAY_WINDOW_MS=0

# --- Reconciliation ---
EBAY_RECON_SNAPSHOT_ON_DRIFT=false
EBAY_THROTTLE_BYPASS=false

# --- Policy Cache ---
EBAY_POLICY_TYPES=shipping,return,payment
EBAY_POLICY_TTL_SEC=3600
EBAY_POLICY_IMPACT_ENABLED=false

# --- Metrics / Suppression (tune) ---
ALERT_SUPPRESSION_WINDOW_SEC=60
ALERT_SUPPRESSION_PER_KEY_OVERRIDE=queue_latency_burn_rate:30

# --- Database (PostgreSQL) ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=ebaytool
PGUSER=ebaytool
PGPASSWORD=__FILL_DB_PASSWORD__    # TODO: local DB password

# --- App ---
NODE_ENV=development
PORT=5000

# --- Optional Logging Tweaks ---
LOG_LEVEL=info

# SECURITY: Never commit this file with secrets. Add /.env to .gitignore if not present.
