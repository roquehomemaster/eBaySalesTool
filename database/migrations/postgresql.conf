# Enable detailed logging for diagnostics
log_min_messages = debug1
log_connections = on
log_disconnections = on
log_statement = 'all'
log_duration = on

# Configure PostgreSQL to listen on the specified IP address
listen_addresses = '192.168.0.220'